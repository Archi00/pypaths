from setuptools import setup, find_packages

setup(
    name="pypaths",
    version="0.1",
    description="Optimized path and temporary directory management utility",
    author="Archi00",
    url="https://github.com/Archi00/pypaths",
    packages=find_packages(),
    python_requires='>=3.8',  # Ensure modern Python features
)